version: "2.4"
services:
    node:
        build:
            context: .
            dockerfile: Dockerfile
        ports: [3000:3000]
        environment:
            - PORT=3000
            - HOST=localhost:3000
        volumes:
            - ./:/usr/src/app/
